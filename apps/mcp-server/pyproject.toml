[project]
name = "mcp-server"
version = "0.1.0"
description = "Vega-AI MCP Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.1.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
