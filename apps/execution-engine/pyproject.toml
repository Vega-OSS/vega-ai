[project]
name = "execution-engine"
version = "0.1.0"
description = "Vega-AI Python Execution Engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.1",
    "grpcio>=1.64.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
